{
  "extends": "next/core-web-vitals",
  "rules": {
    // Disable a specific rule globally
    "no-alert": "off",    // Disable a rule for a specific file or directory
   "react/no-unescaped-entities": "off"
  }
}
